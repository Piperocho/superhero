spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:superherotestdb;DB_CLOSE_DELAY=-1
    username: sa
    password: password
  flyway:
    enabled: true
    locations: db/migration-test
